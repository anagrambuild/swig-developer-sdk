/** @type {import('prettier').Config} */
const config = {
  plugins: ['prettier-plugin-organize-imports'],
  trailingComma: 'all',
  tabWidth: 2,
  semi: true,
  singleQuote: true,
};

export default config;
